import 'package:flutter/material.dart';

/// Paleta oficial do Roluna Connect — visual premium/corporativo.
class AppColors {
  AppColors._();

  // Base
  static const Color primaryDark = Color(0xFF0B1E3D); // azul escuro principal
  static const Color primary = Color(0xFF12305F);
  static const Color primaryLight = Color(0xFF1E4A8C);
  static const Color accent = Color(0xFF2D7DD2); // azul de destaque/ações

  static const Color background = Color(0xFFF5F6F8); // cinza claro
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFEFF2F6);

  // Texto
  static const Color textPrimary = Color(0xFF10192B);
  static const Color textSecondary = Color(0xFF5B6478);
  static const Color textOnDark = Color(0xFFF5F6F8);

  // Prioridades de comunicado
  static const Color normal = Color(0xFF6B7280);
  static const Color importante = Color(0xFFD98E04);
  static const Color urgente = Color(0xFFE0492B);
  static const Color obrigatorio = Color(0xFFB01030);

  // Selo administrador
  static const Color adminBadge = Color(0xFFC79A2E); // dourado sóbrio
  static const Color adminBadgeBg = Color(0xFFFCF3DC);

  // Feedback
  static const Color success = Color(0xFF1E8E5A);
  static const Color divider = Color(0xFFE3E6EC);
  static const Color shadow = Color(0x1A0B1E3D);
}
